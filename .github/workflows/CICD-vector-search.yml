name: CICD-vector-search

on:
  push:
    branches:
      - develop
    paths:
      - "smartwardrobe-AI/vector-search-v1"
  workflow_dispatch:

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout source
        uses: actions/checkout@v3
      - name: Login to docker hub
        run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
      - name: Build Docker Image
        working-directory: ./smartwardrobe-AI/vector-search-v1
        run: docker build -t d23124670/vector-search .
      - name: Publish image to docker hub
        run: docker push d23124670/vector-search:latest
  deploy:
    needs: build
    runs-on: [aws-ec2]
    steps:
      - name: Pull image from docker hub
        run: docker pull d23124670/vector-search:latest
      - name: Delete Old Container
        run: docker rm -f vector-search
      - name: Run docker container
        run: docker run -d -p 5000:5000 --name CN-vector-search d23124670/vector-search:latest
